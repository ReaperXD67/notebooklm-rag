# OpenRouter powers both embeddings and grounded answer generation.
OPENROUTER_API_KEY=sk-or-v1-your-key
OPENROUTER_MODEL=google/gemini-2.5-flash-lite
OPENROUTER_EMBEDDING_MODEL=openai/text-embedding-3-small
# Optional precision-mode judge. Defaults to OPENROUTER_MODEL.
OPENROUTER_RERANK_MODEL=google/gemini-2.5-flash-lite

# Qdrant Cloud is recommended for the live deployed project.
# Local development also works with: docker run -p 6333:6333 qdrant/qdrant
QDRANT_URL=https://your-cluster-url.qdrant.tech
QDRANT_API_KEY=your-qdrant-api-key
QDRANT_COLLECTION=atlaslm_chunks
# Qdrant 1.18+ enables real Google-inspired 4-bit TurboQuant. Older clusters fall back safely.
QDRANT_TURBOQUANT=true

# Short-lived server cache for near-identical questions.
RAG_CACHE_TTL_SECONDS=900
RAG_CACHE_SIMILARITY=0.992

# Optional metadata sent to OpenRouter.
APP_URL=http://localhost:3000
